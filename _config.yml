theme: jekyll-theme-minimal
title: Octocat's homepage
description: Bookmark this to keep an eye on my project updates!

name: create-map
on: push

run: ./Scripts/makefile.sh
runs-on: ubuntu-latest
shell: bash
